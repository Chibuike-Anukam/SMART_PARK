# Dataset root (relative paths work from any machine)
path: moc_parking_lot_datatset
train: train/images
val: val/images
test: test/images

nc: 2
names: ['vacant', 'occupied']

# 0 = vacant, 1 = occupied
